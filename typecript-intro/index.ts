console.log('hola');

let grupo: number = 10;

type operations = 'subtract'|'add'|'divide'|'multiplie'

const resolve = (a:number, b:number, operation:operations):number => {
 
  return 0;
}

resolve(15, 20, 'add');